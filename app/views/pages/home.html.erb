<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Andrew Godeke | Portfolio</title>

  <!-- Devicon CSS for skill icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/devicons/devicon@v2.15.1/devicon.min.css">

  <%= stylesheet_link_tag "application", media: "all" %>
</head>

<body>

<nav class="navbar">
  <!-- Left side: Home button + Theme toggle -->
  <div class="navbar-left">
    <%= link_to root_path, class: 'home-btn', title: 'Home' do %>
      <svg class="home-icon" viewBox="0 0 24 24">
        <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/>
      </svg>
    <% end %>
    <button id="theme-toggle" title="Toggle dark mode">☽</button>
  </div>

  <!-- Right side: Navigation buttons -->
  <div class="navbar-buttons">
    <%= link_to 'About', about_path, class: 'navbar-btn' %>
    <%= link_to 'Projects', projects_path, class: 'navbar-btn' %>
    <%= link_to 'Contact', contact_path, class: 'navbar-btn' %>
  </div>
</nav>

<!-- Hero Section -->
<section class="hero">
  <!-- Left side - Content -->
  <div class="hero-content">
    <div class="content-wrapper">
      <h1 class="hero-greeting">HI, I'M</h1>
      <h2 class="hero-name">ANDREW</h2>
      <p class="hero-description">
        I'm a full-stack web developer with a global edge and a creative mind. I'm here to build meaningful, creative, and human-focused tech. Let's create something impactful.
      </p>
      <%= link_to 'START!', about_path, class: 'cta-button' %>
    </div>
  </div>

  <!-- Right side - Image -->
  <div class="hero-image">
    <%= image_tag "profile.jpg", alt: "Andrew Godeke", class: "profile-image" %>
  </div>
</section>

<script>
  const toggleBtn = document.getElementById('theme-toggle');
  toggleBtn.addEventListener('click', () => {
    document.body.classList.toggle('dark-mode');

    // Change icon when toggling themes
    if (document.body.classList.contains('dark-mode')) {
      toggleBtn.textContent = '☀️'; // Sun icon for light mode toggle
      toggleBtn.title = 'Switch to light mode';
    } else {
      toggleBtn.textContent = '☽'; // Moon icon for dark mode toggle
      toggleBtn.title = 'Switch to dark mode';
    }
  });
</script>

</body>
</html>
